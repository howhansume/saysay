//
//  RoomInfo.m
//  USayApp
//
//  Created by Jong-Sung Park on 10. 7. 1..
//  Copyright 2010 INAMASS.NET. All rights reserved.
//

#import "RoomInfo.h"

@implementation RoomInfo

@synthesize INDEXNO, CHATSESSION, LASTMESSAGEKEY, CTYPE, LASTMSG, LASTMSGTYPE, LASTMSGDATE, LASTUPDATETIME, NEWMSGCOUNT;
@synthesize ISSUCESS, PROCESSTIME;

-(void)dealloc {
	if (INDEXNO) {
		[INDEXNO release];
	}
	if (INDEXNO) {
		[CHATSESSION release];
	}
	if (LASTMESSAGEKEY) {
		[LASTMESSAGEKEY release];
	}
	if (CTYPE) {
		[CTYPE release];
	}
	if (LASTMSG) {
		[LASTMSG release];
	}
	if (LASTMSGTYPE) {
		[LASTMSGTYPE release];
	}
	if (LASTMSGDATE) {
		[LASTMSGDATE release];
	}
	if (LASTUPDATETIME) {
		[LASTUPDATETIME release];
	}
	if (NEWMSGCOUNT) {
		[NEWMSGCOUNT release];
	}
	
	if (ISSUCESS) {
		[ISSUCESS release];
	}
	if (PROCESSTIME) {
		[PROCESSTIME release];
	}
	
	[super dealloc];
}
@end

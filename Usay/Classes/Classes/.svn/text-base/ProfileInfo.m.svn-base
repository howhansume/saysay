//
//  ProfileInfo.m
//  USayApp
//
//  Created by Jong-Sung Park on 10. 7. 11..
//  Copyright 2010 INAMASS.NET. All rights reserved.
//

#import "ProfileInfo.h"


@implementation ProfileInfo		
@synthesize INDEXNO;			
@synthesize PKEY;							
@synthesize FORMATTED;		
@synthesize STATUS;				
@synthesize STATUSSP;			
@synthesize NICKNAME;			
@synthesize NICKNAMESP;			
@synthesize ORGANIZATION;		
@synthesize EMAILADDRESS;		
@synthesize IPHONENUMBER;		
@synthesize MAINPHONENUMBER;	
@synthesize MOBILEPHONENUMBER;	
@synthesize HOMEPHONENUMBER;	
@synthesize ORGPHONENUMBER;		
@synthesize HOMEPAGEURL;		
@synthesize BIRTHDAY;			
@synthesize IMSTATUS;			
@synthesize REPRESENTPHOTO;	
@synthesize REPRESENTPHOTOSP;	
@synthesize PHOTOS;			

-(void)dealloc{
	if(INDEXNO)				[INDEXNO			release];				
	if(PKEY)				[PKEY				release];						
	if(FORMATTED)			[FORMATTED			release];				
	if(STATUS)				[STATUS				release];		
	if(STATUSSP)			[STATUSSP			release];	
	if(NICKNAME)			[NICKNAME			release];
	if(NICKNAMESP)			[NICKNAMESP			release];		
	if(ORGANIZATION)		[ORGANIZATION		release];		
	if(EMAILADDRESS)		[EMAILADDRESS		release];		
	if(IPHONENUMBER)		[IPHONENUMBER		release];
	if(MAINPHONENUMBER)		[MAINPHONENUMBER	release];
	if(MOBILEPHONENUMBER)	[MOBILEPHONENUMBER	release];				
	if(HOMEPHONENUMBER)		[HOMEPHONENUMBER	release];			
	if(ORGPHONENUMBER)		[ORGPHONENUMBER		release];			
	if(HOMEPAGEURL)			[HOMEPAGEURL		release];				
	if(BIRTHDAY)			[BIRTHDAY			release];		
	if(IMSTATUS)			[IMSTATUS			release];	
	if(REPRESENTPHOTO)		[REPRESENTPHOTO		release];	
	if(REPRESENTPHOTOSP)	[REPRESENTPHOTOSP	release];
	if(PHOTOS)				[PHOTOS				release];

	
	[super dealloc];
}

@end
